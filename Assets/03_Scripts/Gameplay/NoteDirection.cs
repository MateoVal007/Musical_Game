// Las 4 direcciones posibles de una nota-flecha. El jugador tiene que
// flexionar la muñeca en esta dirección para resolverla (ver DrumHitDetector).
public enum NoteDirection
{
    Up,
    Down,
    Left,
    Right
}
